import UIKit

protocol {{ module_info.name }}ViewInput: BaseViewInput {

}

class {{ module_info.name }}ViewController: BaseViewController {

	// MARK: - Constants -

	// MARK: - Variables -

	fileprivate var customView: {{ module_info.name }}View { return forceCast(view) }
	var output: {{ module_info.name }}ViewOutput?
    override var basePresenter: BasePresenterInput? { return output }

	// MARK: - Initialization -

	override func viewDidLoad() {
		super.viewDidLoad()
		setupView()
		setupActions()
	}

	private func setupView() {

	}

	private func setupActions() {

	}

	override func loadView() {
		view = {{ module_info.name }}View()
	}

}

extension {{ module_info.name }}ViewController: {{module_info.name}}ViewInput {

}
